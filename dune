(rule
  (target command_converse.zip)
  (deps 
    src/lib.ml
    src/lib.mli
    src/server.ml
    src/client.ml
    src/dune
    tests/tests.ml
    tests/dune
  )
  (action
    (run zip -R command_converse.zip %{deps})))
